const tpl: string = `
<div class="{{stl.avatar}}" >{{{image}}}</div> 
<form action="">
    {{{oldPassword}}}
    {{{newPassword}}}
    {{{newPasswordOneMore}}}
    <p>{{{submit}}}</p>
</form>

`;

export default tpl;
