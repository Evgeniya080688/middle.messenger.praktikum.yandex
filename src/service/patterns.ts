export enum Patterns {
    login = '^[a-zA-Z][a-zA-Z0-9-_\\-]{3,20}$',
    first_name = '',
    second_name = '',
    email = '/^[a-zA-Z\\-0-9]+@+(([a-zA-Z\\-]+[a-zA-Z]\\.)+[a-zA-Z]{2,})$/',
    password = '',
    phone = '',
    message = ''

}
